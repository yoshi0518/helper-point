import { HelpersTemplate } from '@/features/helpers/components/server/HelpersTemplate';

import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'お手伝い登録 | Helper Point',
};

const Page = () => <HelpersTemplate />;

export default Page;
